function solution(seoul) {
     let result = seoul.indexOf('Kim');

    return `김서방은 ${result}에 있다`
}


function solution(seoul) {
     let result = seoul.findIndex( a => a === 'Kim');
    
    return `김서방은 ${result}에 있다`
}
